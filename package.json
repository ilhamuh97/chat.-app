{
  "name": "chat-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm run install:be && npm run install:fe",
    "install:be": "npm install --prefix backend",
    "install:fe": "npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install && npm run setup && npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "preview": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}